# 财务文件智能分析产品 TODO List

目标：从当前阶段推进到可交付给 100 人以内四大投资建议/交易咨询人员使用的个人 AI 助理 MVP。当前默认方案为飞书机器人上传优先，第一个能力是材料财务摘要，核心层需要保留后续扩展更多 AI 助理能力的口子。

## 0. 当前决策对齐

- [x] 确认 MVP 第一入口为飞书机器人消息。
- [x] 确认首批目标用户为 100 人以内的四大投资建议/交易咨询人员，主要诉求是客观反馈材料数据，辅助用户判断是否投资。
- [x] 确认产品定位：为受企业网络安全管控的专业人员提供外部网络个人 AI 助理服务，材料分析只是第一个能力。
- [x] 确认首批支持文件类型：Excel、CSV、可解析 PDF。
- [x] 确认图片、扫描件、图片型 PDF 暂不作为首批必做能力，OCR 后续按需接入。
- [x] 确认单文件大小上限：MVP 先限制为 20MB。
- [x] 确认首批分析场景：用户上传财务资源文件后生成财务摘要，不优先做银行流水、发票、合同或科目余额表专项分析。
- [x] 确认大模型供应商：MVP 暂定通义千问，但必须通过通用 LLM Provider 封装实现，后续只改 `baseURL`、`apiKey/appId`、`model` 等配置即可切换供应商。
- [x] 确认允许将文件内容发送给第三方大模型 API，但必须在隐私政策和用户提示中明确说明，并确认供应商默认不用于模型训练。
- [x] 确认数据留存策略：默认原始文件 30 天、解析文本 30 天、分析报告 180 天；实际策略通过数据库配置控制，由服务内定时任务自动清理。

## 1. 产品与交互设计

- [ ] 完成 `prd.md` 的待确认问题对齐。
- [ ] 定义 MVP 的用户主流程：向飞书机器人发送文件、收到含模型名的首轮确认、收到分步进度推送、收到 Markdown 格式分析结果。
- [ ] 定义飞书机器人交互文案（含模型名、步骤进度、失败提示）。
- [ ] 设计 Markdown 报告结构：文件概览、一句话摘要、财务要点摘要、全文概要、客观数据表。
- [ ] 设计任务失败时的提示文案和重试入口。
- [ ] 设计用户反馈入口：准确、不准确、补充说明。
- [ ] 设计隐私提示和免责声明文案。
- [ ] 明确 MVP 不支持能力，并在界面中提示，例如扫描件识别、多文件合并、自动凭证生成。

## 2. 技术方案确认

- [x] 确认技术栈：FastAPI、LangGraph/LangChain、MySQL、Redis、Celery、React/Vite、飞书消息/卡片模板。
- [x] 确认 AI 编排框架：优先使用 LangGraph、LangChain 等成熟框架，不自研 Agent 框架。
- [x] 确认 MVP 运行方式：本机常驻飞书长连接 worker 进程；阿里云 ECS + Docker Compose 作为后续多人稳定使用方案。
- [x] 确认后续 ECS 初始配置：4 核 8GB、100GB 磁盘、5Mbps 固定带宽。
- [x] 确认 MVP 暂不使用阿里云 OSS，用户文件先存 ECS 本地临时目录，允许按日自动清理。
- [x] 确认缓存和任务队列使用 Redis，异步任务使用 Celery + Redis。
- [x] 确认日志方案：容器标准输出 + 文件日志 + 结构化 JSON 日志 + 飞书错误告警，MVP 暂不接入复杂日志平台。
- [x] 确认数据库选型：MySQL，优先使用支持 JSON 字段的版本。
- [x] 确认文件解析库：CSV 使用 Python 标准库或 pandas；Excel 使用 openpyxl/pandas；可解析 PDF 使用 pypdf；扫描件 PDF 不在 MVP 范围。
- [x] 确认大模型调用封装方式和 Prompt 版本管理方式：统一 `LLMProvider` 接口，供应商信息全部配置化；通过 `PromptVersion` 记录提示词版本。
- [x] 确认 AI 能力扩展机制：使用 `Capability`、`CapabilityVersion`、`AssistantTask` 承载不同个人助理能力。

## 3. 飞书入口接入

- [ ] 做飞书机器人入口可行性验证 Spike：验证能否接收文件、下载文件、识别用户、发送结果通知。
- [x] 搭建飞书事件回调 FastAPI 骨架，支持 health check、challenge 响应、verification token 校验和文件消息识别。
- [x] 启动本地 FastAPI 服务并通过 cloudflared 暴露临时 HTTPS 回调地址，已验证 `/health` 和飞书 `challenge` 响应。
- [x] 改为优先使用飞书官方 SDK 长连接接收消息事件，HTTP 回调作为备选方案。
- [x] 确认飞书入口采用机器人消息方案；MVP 直接用飞书 Markdown 文本回复结果，H5 结果页作为后续增强。
- [x] 创建飞书开放平台应用，已取得 app id 和 app secret。
- [ ] 配置飞书应用权限：接收消息、读取文件、发送消息。
- [ ] 配置飞书事件订阅方式为“使用长连接接收事件”。
- [ ] 实现飞书回调签名校验。
- [x] 实现飞书用户身份识别。
- [x] 实现飞书文件消息接收。
- [x] 实现飞书文件下载到后端。
- [x] 实现收到文件后的即时回复：“已收到文件，正在分析”。
- [ ] 实现分析完成后的飞书结果通知。
- [ ] 实现分析失败后的飞书失败通知。

## 4. 核心后端能力

- [ ] 搭建后端 API 项目。
- [ ] 设计并创建数据库表：用户、入口账号、文件、上传请求、分析任务、解析结果、分析报告、反馈。
- [ ] 设计并创建能力扩展表：`Capability`、`CapabilityVersion`。
- [ ] 实现内部用户模型。
- [ ] 实现入口账号映射模型 `ChannelAccount`。
- [ ] 实现统一上传请求模型 `UploadRequest`。
- [ ] 实现文件元信息保存。
- [ ] 实现本地临时文件存储服务，支持按数据留存配置自动清理。
- [ ] 实现分析任务创建。
- [ ] 实现任务类型字段 `capability_code` 和 `capability_version`，MVP 默认 `material_financial_summary`。
- [ ] 实现任务状态流转：已上传、解析中、分析中、成功、失败。
- [ ] 实现任务失败原因记录。
- [ ] 实现任务重试机制。
- [ ] 实现结果查询 API。
- [ ] 实现用户反馈 API。
- [ ] 实现系统配置表，用于控制数据留存天数、清理开关和清理批次大小。
- [ ] 实现数据清理定时任务，按数据库配置自动清理过期原始文件、解析文本和分析报告。

## 5. 文件解析能力

- [ ] 实现 CSV 解析。
- [ ] 实现 Excel 解析，支持多 Sheet、表头识别、基础统计。
- [ ] 实现可解析 PDF 文本提取。
- [x] 实现可解析 PDF 文本提取，并保留页码用于引用。
- [ ] 保存解析后的文本内容。
- [ ] 保存解析后的结构化表格内容。
- [ ] 保存解析元信息，例如页数、Sheet 数、行数、列数、解析耗时。
- [ ] 处理不支持文件类型的错误提示。
- [ ] 处理超大文件、空文件、损坏文件的错误提示。
- [ ] 为后续 OCR 接入预留解析接口。
- [ ] 接入扫描件/图片型 PDF OCR：当前 `250626 KP 2024 Audited Accounts.pdf` 这类无文本层 PDF 会提取 0 字符，需要 OCR 或多模态模型能力后才能分析。

## 6. 大模型分析能力

- [ ] 设计财务摘要 Prompt，要求只反馈材料中的客观结果，不臆造材料内容。
- [x] 实现通义千问兼容的 `LLMProvider`，支持通过环境变量配置 `baseURL`、`apiKey`、`model` 等。
- [x] 实现财务摘要 Prompt 初版，要求输出文件概览、一句话摘要、财务要点摘要、全文概要、客观数据表。
- [ ] 设计不同文件类型的分析策略：表格类、文本类、合同类。
- [ ] 实现解析内容切片和压缩，避免超过模型上下文。
- [ ] 实现大模型 API 调用封装，支持通过配置切换 `baseURL`、`apiKey/appId`、`model`、`timeout`、`maxTokens`、`temperature`。
- [ ] 使用 LangGraph、LangChain 等成熟框架编排解析、摘要、结构化输出和校验流程。
- [ ] 要求模型输出结构化 JSON。
- [ ] 实现模型输出 JSON schema 校验。
- [ ] 实现失败重试和超时控制。
- [ ] 实现财务或经营相关客观数据提取，用作后续投资分析和风险判定。
- [ ] 将客观数据提取结果和模型摘要结果合并生成报告。
- [ ] 保存模型名称、Prompt 版本、调用耗时和 token 成本。
- [ ] 设计分片分析缓存：以文件哈希、解析文本哈希、分片索引、Prompt 版本、模型名和参数生成 cache key，命中时复用分片结果，避免重复消耗 token。
- [ ] 记录每次模型调用的 token 用量：input tokens、output tokens、total tokens、估算成本、耗时和供应商 request id。
- [x] 在每次任务完成后向用户返回本次解析与生成任务的 token 汇总，包括 input、output、total。

## 6.1 上下文与记忆管理（后续阶段，短期内先不实现）

- [ ] 短期策略：MVP 先只保留当前任务内上下文，不做长期记忆写入；先把文件级摘要、分片摘要和最终报告持久化，为后续追问做准备。
- [ ] 设计会话上下文模型 `ConversationContext`：记录用户当前任务、最近文件、最近报告、追问历史和可引用的中间结果。
- [ ] 设计长期记忆模型 `UserMemory`：只保存用户明确确认的偏好、常用分析口径、行业关注点和固定输出格式，不默认保存敏感材料内容。
- [ ] 设计文件级记忆模型 `DocumentMemory`：保存文件摘要、关键财务指标、页码引用、结构化数据表和可追问索引。
- [ ] 设计记忆写入策略：文件解析完成后先生成候选记忆，返回给用户确认“保留/舍弃/仅本次会话使用”。
- [ ] 设计记忆淘汰策略：按敏感级别、有效期、用户确认状态、最近使用时间清理，避免上下文无限膨胀。
- [ ] 设计追问上下文组装策略：优先放入当前文件摘要、相关页码证据、用户明确保留的偏好，再按 token budget 选择历史记忆。
- [ ] 设计记忆可视化提示：每次文件解析完成后告知当前使用了哪些记忆、建议新增哪些记忆、哪些旧记忆可舍弃。
- [ ] 增加隐私控制：支持用户指令“忘记这个文件/忘记这个项目/清空我的记忆”。

## 6.2 Agent 能力扩展

- [ ] 设计能力注册表 `CapabilityRegistry`：每个能力包含 code、名称、触发条件、输入 schema、输出 schema、Prompt 版本和执行器。
- [ ] 设计关键词/意图路由：基于规则先做高确定性命中，例如“总结文件”“提取数据表”“生成尽调问题”，低确定性时再调用模型分类。
- [ ] 设计固定 skill 执行机制：命中能力后执行对应工作流，例如文件摘要、指标抽取、风险点整理、尽调问题生成、报告格式转换。
- [ ] 设计多轮追问工作流：用户追问时先判断是否引用当前文件、历史文件或长期记忆，再选择对应能力执行。
- [ ] 为 LangGraph/LangChain 编排预留节点：路由、检索、分片缓存、模型调用、校验、记忆写入和飞书回复。

## 7. 飞书 Markdown 报告回复

- [ ] 实现飞书 Markdown 格式报告回复。
- [x] 实现飞书 Markdown 解析预览回复；已接入模型路径，配置 `LLM_API_KEY` 后返回正式财务摘要。
- [ ] 展示文件基本信息和任务状态。
- [ ] 展示文件概览。
- [ ] 展示一句话摘要。
- [ ] 展示财务要点摘要。
- [ ] 展示全文概要，包括全文观点和观点所在章节、页码、Sheet 或段落。
- [ ] 展示客观数据表，包括指标名、数值、单位、期间、主体和来源位置。
- [ ] 展示原始数据引用。
- [ ] 对材料中无法确认的信息展示“未在材料中发现”或“不足以判断”。
- [ ] 支持用户直接复制飞书消息中的报告文本。
- [ ] 支持用户反馈入口。
- [ ] 若报告内容过长，支持拆分多条消息或生成 Markdown 附件。
- [x] 优先优化大报告体验：生成 `.md` 文件并通过飞书文件消息推送，飞书消息正文只保留简短摘要、文件名、模型名和 token 汇总。
- [x] 实现飞书文件发送：调用 `/im/v1/files` 上传报告文件获取 `file_key`，再通过文件消息回复用户；文件类型优先使用 `stream`。
- [x] 设置报告发送策略：短报告直接 Markdown 文本回复，超过阈值时发送 `.md` 附件，避免多条长消息刷屏。
- [ ] 使用真实飞书消息实测 `.md` 附件上传和文件回复权限，确认是否需要补充 `im:resource` 或 `im:resource:upload` 权限。

## 8. 安全与数据保护

- [ ] 全站启用 HTTPS。
- [ ] 飞书回调接口校验签名。
- [ ] 文件下载链接设置有效期。
- [ ] 原始文件不公开访问。
- [ ] 日志中避免记录完整财务内容。
- [ ] 对手机号、身份证号、银行卡号等敏感信息做日志脱敏。
- [ ] 增加文件大小限制。
- [ ] 增加文件类型白名单。
- [ ] 提供用户手动删除原始文件和分析结果的能力。
- [ ] 自动清理任务需要记录清理日志，包含清理对象、数量、时间和失败原因。
- [ ] 配置数据库备份。
- [ ] 配置服务器磁盘快照。
- [ ] 编写隐私政策。
- [ ] 编写用户协议。
- [ ] 编写 AI 结果免责声明。

## 9. 部署与运维

- [ ] 后续多人稳定使用时再购买或准备阿里云 ECS。
- [ ] 配置域名和 HTTPS 证书。
- [ ] 后续部署阶段配置 Docker 和 Docker Compose。
- [ ] 编写 `docker-compose.yml`。
- [ ] 配置 `nginx` 反向代理。
- [ ] 配置后端环境变量。
- [ ] 配置数据库初始化脚本。
- [ ] 配置 Redis。
- [ ] 配置日志目录和日志轮转。
- [ ] 配置数据库定时备份。
- [ ] 配置基础监控：CPU、内存、磁盘、容器状态。
- [ ] 配置错误告警，例如飞书通知或邮件通知。
- [ ] 编写部署文档。
- [ ] 编写回滚方案。

## 10. 测试与验收

- [ ] 准备测试文件：CSV、Excel、可解析 PDF、损坏文件、超大文件、不支持格式。
- [ ] 测试飞书上传成功流程。
- [ ] 测试飞书上传失败流程。
- [ ] 测试文件解析成功流程。
- [ ] 测试文件解析失败流程。
- [ ] 测试大模型分析成功流程。
- [ ] 测试大模型超时或失败流程。
- [ ] 测试任务重试。
- [ ] 测试飞书 Markdown 报告回复。
- [ ] 测试长报告拆分多条消息或 Markdown 附件返回。
- [ ] 测试并发 5 个分析任务。
- [ ] 测试 20MB 文件上传和处理。
- [ ] 验证报告中的摘要、观点和客观数据都带有数据来源引用。
- [ ] 验证系统不会臆造材料中不存在的信息。
- [ ] 验证从上传到收到结果通知的完整闭环。

## 11. 用户交付

- [ ] 准备第一批试用用户名单。
- [ ] 准备面向四大投资建议/交易咨询人员的用户使用说明。
- [ ] 准备支持文件类型说明。
- [ ] 准备不支持能力说明。
- [ ] 准备常见问题 FAQ。
- [ ] 准备用户反馈收集表。
- [ ] 邀请 3 到 5 个种子用户试用。
- [ ] 收集第一轮真实文件和真实反馈。
- [ ] 修复阻塞用户完成分析的关键问题。
- [ ] 邀请 20 人以内小范围试用。
- [ ] 根据试用反馈调整摘要模板和报告展示。
- [ ] 达到稳定后开放给 100 人以内用户。

## 12. 后续迭代

- [ ] 接入 OCR，支持图片、扫描件和图片型 PDF。
- [ ] 评估 Web 管理台可行性，仅在确认用户环境不会拦截时再做。
- [ ] 增加多文件合并分析。
- [ ] 增加专项分析模板：银行流水、报销、发票、合同、科目余额表。
- [ ] 扩展更多个人 AI 助理能力，例如材料问答、尽调问题清单、投资备忘录草稿、经营数据对比、风险线索整理、工作清单生成。
- [ ] 增加报告导出。
- [ ] 增加成本统计和用量限制。
- [ ] 增加企业组织、成员、角色和权限。
- [ ] 增加微信小程序、企业微信、钉钉、邮箱等入口。
- [ ] 评估是否迁移到托管数据库、托管 Redis 和独立 Worker。

## 13. MVP 完成标准

- [ ] 用户可以向飞书机器人发送文件。
- [ ] 系统可以自动创建分析任务。
- [ ] 系统可以解析 CSV、Excel、可解析 PDF。
- [ ] 系统可以调用大模型生成结构化报告。
- [ ] 用户可以收到飞书完成通知。
- [ ] 用户可以在飞书中收到 Markdown 格式报告。
- [ ] 报告包含文件概览、一句话摘要、财务要点摘要、全文概要、客观数据表和免责声明。
- [ ] 任务失败时有明确错误原因。
- [ ] 管理者可以查看日志并定位失败原因。
- [ ] 数据有备份，原始文件不公开暴露。
- [ ] 3 到 5 个种子用户能完成真实文件分析闭环。
